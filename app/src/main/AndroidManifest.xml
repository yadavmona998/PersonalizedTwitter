<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mona.personalizedtwitter" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InbuildWebView"
            android:label="@string/title_activity_inbuild_web_view" >
        </activity>
        <activity
            android:name=".TweetReply"
            android:label="@string/title_activity_tweet_reply" >
        </activity>
        <activity
            android:name=".TweetDetailsActivity"
            android:label="@string/title_activity_tweet_details" >
        </activity>
    </application>

</manifest>
